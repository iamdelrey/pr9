plugins {
    id 'java'
}

group 'org.example'
version '1.0'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.7.0'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.7.0'
}

test {
    useJUnitPlatform()
}

jar {
    manifest {
        attributes 'Main-Class': 'org.example.Task.App'
    }
}


task task1 {
    task1.dependsOn jar
    copy {
        from jar
        into 'docker'
    }
    finalizedBy {
        Docker
    }
}

task buildDockerImage(type: Exec) {
    commandLine "docker", "build", "-t", "${project.name}:${project.version}", "-f", "docker/Dockerfile", "."
}

task runDockerContainer(type: Exec) {
    dependsOn buildDockerImage
    commandLine "docker", "run", "-t", "docker.io/library/${project.name}:${project.version}"
}
//task Docker(type: Exec) {
//    doFirst {
//        commandLine "docker", "build", "-f", "docker/Dockerfile", "-t", "${project.name}:${project.version}", "."
//    }
//    doLast {
//        commandLine "docker", "run", "-t", "${project.name}:${project.version}"
//    }
//}